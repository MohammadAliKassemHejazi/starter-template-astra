# Jules Lessons

> Read automatically by `run-jules-task.mjs create` and appended to every new
> prompt sent to Jules, so a mistake caught once is never repeated. Written by
> whichever specialist reviews a Jules PR and fixes an issue in it — mandatory,
> not optional (`jules-delegation-reference.md`). Cap ~150 lines; roll the
> oldest half into a dated summary when exceeded (same rule as
> `ceo-memory.md`/`decision-log.md` — `token-efficiency-reference.md`'s
> Memory & log hygiene).

*(no lessons recorded yet)*
