# Escalation — ESC-<id>

> **A subagent's ONLY way to pause on genuine ambiguity, instead of guessing.**
> A dispatched specialist has no interactive prompt back to the Founder and no
> conversation-history context beyond its brief — when a brief genuinely
> doesn't cover the situation (a real architectural fork, a missing env var,
> conflicting instructions), the RIGHT move is to write this file and STOP
> cleanly, not invent an answer. Filed in `company/escalations/`, named
> `ESC-<sequential-id>.md`. Team Lead checks this folder as part of its normal
> operating loop (`escalation-watch.mjs` reminds if one goes unaddressed).

**Filed by:** <agent> | **Date:** YYYY-MM-DD HH:MM UTC | **Story:** US-NN
**Status:** 🔴 Open / 🟡 Answered, resuming / ✅ Resolved

## What I was doing
<The task, one line — link the brief/story.>

## Exactly what's blocking me
<Be specific. "The brief says use packages/shared/src/schemas/user.ts but that file exports
BOTH UserRequest and UserProfile — which one does 'the user contract' mean
here?" — not "I'm not sure what to do.">

## What I already tried
<Checked the brief / CONVENTIONS.md / the file it said to mirror / project-graph
for consumers — show the ambiguity survived a real attempt to resolve it
yourself, not just "I didn't look.">

## Options I see (if any)
1. <option — with the trade-off>
2. <option — with the trade-off>

## What I did instead of guessing
Stopped cleanly. No files left half-edited in a broken state — <confirm this:
"reverted my one touched file" / "no files touched yet".>

## Resolution (filled by Team Lead / whoever answers)
<Answer, then update Status above and notify the filing agent to resume.>
