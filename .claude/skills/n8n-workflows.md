# Skill: n8n / Make / Zapier Workflow Automation

Load when a client automation should live in a workflow platform instead of custom code.

Full doctrine, checklists, and failure modes: `n8n-workflows-reference.md`.
