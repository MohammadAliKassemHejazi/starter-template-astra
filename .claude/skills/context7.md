# Skill: Context7 — Live Library Docs (MCP)

Load when working with fast-moving libraries where trained knowledge is likely stale: Next.js, Prisma, LangChain, Expo, Tailwind, tRPC, Drizzle, AI SDKs.

**Requires**: Context7 MCP server connected. If absent → fallback: web-fetch the official docs for the EXACT version in `package.json`; note the missing server in the daily log (connecting MCP servers is the Founder's config action).

Full doctrine, checklists, and failure modes: `context7-reference.md`.
