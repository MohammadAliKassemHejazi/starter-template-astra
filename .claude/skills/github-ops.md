# Skill: GitHub Operations (MCP)

Load for PR creation/review, issue triage, or release cycles on the client repo.

**Requires**: GitHub MCP server. If absent → fallback: local `git` + `gh` CLI; if neither, prepare branch + commit + PR description text for the Founder to execute.

Full doctrine, checklists, and failure modes: `github-ops-reference.md`.
