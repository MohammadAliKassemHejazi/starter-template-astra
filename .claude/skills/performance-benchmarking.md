# Skill: Performance Benchmarking

Load for Core Web Vitals work, bundle analysis, or load testing. Pairs with
`css-architecture.md` (render performance), `vercel.md`/`cloudflare.md` (edge/caching),
and feeds `growth-seo-specialist` (CWV is a ranking + UX signal).

Full doctrine, checklists, and failure modes: `performance-benchmarking-reference.md`.
