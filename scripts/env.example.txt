# Template for .env (harness denied writing .env.example; copy to .env.example if desired).
# Use `npm run setup` (generates random secrets). Never commit .env.
# Values marked CHANGE_ME are replaced by scripts/init-env.mjs.

# --- Postgres (docker compose db service) ---
POSTGRES_USER=app
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=app
# Host port, bound to 127.0.0.1 only
DB_HOST_PORT=5433
# Inside compose the host is `db`. ${VAR} expansion is done by docker compose.
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
TEST_DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/app_test

# --- Server ---
NODE_ENV=development
LOG_LEVEL=info
# >= 32 chars
JWT_ACCESS_SECRET=CHANGE_ME
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_DAYS=7
ARGON2_MEMORY_KIB=19456
ARGON2_TIME_COST=2
ALLOW_REGISTRATION=true
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=CHANGE_ME
CLIENT_ORIGIN=http://localhost:3000
TRUST_PROXY=1
RATE_LIMIT_GENERAL_MAX=300
# For an e2e run only, override on the command line (do not edit here):
#   RATE_LIMIT_AUTH_MAX=10000 docker compose up -d --force-recreate server
RATE_LIMIT_AUTH_MAX=20
RATE_LIMIT_WINDOW_MS=900000

# --- Client (baked into the Next build as a rewrite target; rebuild after changing) ---
API_ORIGIN=http://server:4000

# Host ports (change if 3000/4000 are taken; CLIENT_ORIGIN must match the client port)
# CLIENT_HOST_PORT=3000
# SERVER_HOST_PORT=4000
